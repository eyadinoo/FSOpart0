```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: HTTP POST request to address new_note_spa
    Note right of browser: The browser adds note to notes list and rerenders the list
    activate server
    server-->>browser: status code 201
    deactivate server
```